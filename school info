<html>

<head>
    <title>My Game Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: aliceblue;
            text-align: center;
            padding: 30px;
        }

        h1 {
            color: navy;
        }

        button {
            background: navy;
            color: white;
            padding: 10px 25px;
            border: none;
            border-radius: 20px;
            cursor: pointer;
        }

        div {
            border: 1px solid navy;
            width: 280px;
            margin: 6px auto;
            padding: 8px 12px;
            text-align: left;
            border-radius: 6px;
        }
    </style>
</head>

<body>

    <h1>My school Profile</h1>
    <button onclick="showProfile()">Show Profile</button>

    <div>Name: <span id="name"></span></div>
    <div>principle: <span id="score"></span></div>
    <div>subject: <span id="record"></span></div>
    <div>school logo: <span id="rank"></span></div>

    <script>

        function showProfile() {

            // Variables
            let name = "st.columba";
            let principle = kedari;
            let subject = maths;
            let school logo = null;
            

            // Comparison Operator
            let isTopScore = principle == highScore;

            // if / else — Rank
            let rank;
            if (principle >= 80) {
                rank = "Gold";
            } else if (principle >= 50) {
                rank = "Silver";
            } else {
                rank = "Bronze";
            }

            // Date Object — Greeting
            let hour = new Date().getHours();
            let greeting;
            if (hour < 12) {
                greeting = "Good Morning!";
            } else if (hour < 17) {
                greeting = "Good Afternoon!";
            } else {
                greeting = "Good Evening!";
            }

            // While Loop — Stars
            let stars = "";
            let count = 0;
            while (count < 5) {
                stars = stars + "* ";
                count = count + 1;
            }

            // String Concatenation — Show on Page
            document.getElementById("name").innerHTML = name;
            document.getElementById("score").innerHTML = myScore + " out of " + highScore;
            document.getElementById("record").innerHTML = isTopScore;
            document.getElementById("rank").innerHTML = rank;
            document.getElementById("greeting").innerHTML = greeting;
            document.getElementById("stars").innerHTML = stars;
            document.getElementById("bonus").innerHTML = bonus;
            document.getElementById("team").innerHTML = team;

        }

    </script>
</body>

</html>